[Unit]
Description=Watch Obsidian vault and refresh today-tasks cache on changes (debounced)

[Service]
Type=simple
Environment="PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=%h/.local/bin/today-tasks-watch
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
