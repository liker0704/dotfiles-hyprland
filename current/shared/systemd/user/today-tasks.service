[Unit]
Description=Aggregate today's tasks (vault + GCal) into ~/.cache/today-tasks.{txt,json}

[Service]
Type=oneshot
Environment="PATH=%h/.local/bin:/usr/local/bin:/usr/bin:/bin"
ExecStart=%h/.local/bin/today-tasks

[Install]
WantedBy=default.target
